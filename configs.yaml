# configs.yaml
# directory paths
# *** NOTE: these paths are specific to the Harvard ITC cluster and should be changed for local use
catalogue_dir: "/n/holystore01/LABS/itc_lab/Lab/galaxyGNN"

# artifact roots -- heavy outputs live OUTSIDE the code repo (see REFACTOR_PLAN.md)
#   data_root: processed graphs + tabular data + stats
#   out_root:  checkpoints + results
# (~ is expanded; on a laptop point these wherever you keep the artifacts)
data_root: "~/halognn"
out_root: "~/halognn"

# params
project_dir: "."
model_type: "GNN"
sim: "ASTRID"
z: 4
observables_only: False
graph_radius: 2000.0
min_mass: 1e7
hm_present: False

# params: INFO, DEBUG
logging:
  level: INFO